apiVersion: v1
kind: Service
metadata:
  name: {{ include "irl.backend.fullname" . }}
  labels:
    {{- include "irl.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "irl.backend.selectorLabels" . | nindent 4 }}
